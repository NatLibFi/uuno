whoami = 'normal'           # 'normal' or 'vapaakappale'
app_host = ''		    # IP address of the URN resolver
app_port =                  # Port we listen to (integer).

# PostgreSQL configuration (you might not need all the parameters).
db_config = {
    'dbname': '',
    'user': '',
    'password': '',
    'host': '',
    'port': '',
}
